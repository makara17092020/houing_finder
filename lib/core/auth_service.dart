class AuthService {
  // Simple flag to track authentication state. In a real app, this would be
  // backed by secure storage / provider and include more information.
  static bool isLoggedIn = false;
}
